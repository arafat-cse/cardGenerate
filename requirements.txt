fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pillow>=10.4
qrcode[pil]>=7.4
reportlab>=4.2

# Background removal (local AI model, ~250 MB one-time download on first use).
# start.bat installs this automatically. If it fails on your machine, the rest
# of the app still works - only the "Remove Background" button is disabled.
# rembg[cpu]>=2.0.57
